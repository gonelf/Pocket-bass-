# Payload CMS Configuration
PAYLOAD_SECRET=your-secret-key-here-min-32-chars

# Server Configuration
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
NEXT_PUBLIC_BASE_DOMAIN=yourdomain.com

# Multi-Tenant Configuration
# Generate with: openssl rand -base64 32
PROVISIONING_SECRET=your-provisioning-secret-here-min-32-chars

# Database Configuration (Turso SQLite)
# Sign up at https://turso.tech and create a database
# Run: turso db create pocket-bass
# Run: turso db show pocket-bass --url
DATABASE_URI=libsql://your-database.turso.io
# Run: turso db tokens create pocket-bass
DATABASE_AUTH_TOKEN=your-turso-auth-token

# Alternative: Use local SQLite for development
# DATABASE_URI=file:./data/payload.db

# Cloudflare R2 Storage Configuration
# Create a bucket in Cloudflare R2 dashboard
# Generate API tokens in Cloudflare dashboard
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
R2_BUCKET_NAME=pocket-bass-media

# Email Configuration (Optional - for user verification/password reset)
# You can use services like Resend, SendGrid, etc.
# EMAIL_FROM=noreply@yourdomain.com
# EMAIL_PROVIDER=resend
# EMAIL_API_KEY=your-email-api-key
